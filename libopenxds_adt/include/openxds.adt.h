#ifndef OPENXDS_ADT_H
#define OPENXDS_ADT_H

#include <openxds/types.h>
#include <openxds.core/types.h>

namespace openxds {
	namespace adt {
	
template<class E> class PUBLIC IDictionary;
template<class E> class PUBLIC IEIterator;
template<class E> class PUBLIC IEntry;
template<class E> class PUBLIC IGeneralTour;
template<class E> class PUBLIC IIterator;
template<class E> class PUBLIC IList;
template<class E> class PUBLIC IMap;
template<class E> class PUBLIC IPIterator;
template<class E> class PUBLIC IPosition;
template<class E> class PUBLIC IStack;
template<class E> class PUBLIC ISequence;
template<class E> class PUBLIC ITree;

//template<class E> class PUBLIC IEntry;
//template<class E> class PUBLIC Dictionary;
//template<class E> class PUBLIC List;

};};

#endif
